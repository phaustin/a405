---
jupytext:
  formats: ipynb,md:myst
  text_representation:
    extension: .md
    format_name: myst
    format_version: 0.13
    jupytext_version: 1.19.1
kernelspec:
  display_name: Python 3 (ipykernel)
  language: python
  name: python3
---

(assignment5_thompkins)=
# Assignment 5: Thompkins  questions

Hand in pdfs, tephigrams or scanned images that show all your work

Link to [assigment5_thompkins.md](https://drive.google.com/file/d/1xIPs9Nl4Fo253WMs4LwzIbW-QEmdG8e8/view?usp=sharing)

+++

## 1. Buoyancy and stability


1.i)  (3pt) Our by-now familiar air parcel of moist (but non-cloudy)
air has a temperature of 24.9 deg C. It is neutrally buoyant sitting in an
environment that has a temperature of 25 deg C and a mixing ratio of 10
$g kg^{-1}$. What is the mixing ratio in the air parcel?

```{code-cell} ipython3
# 1.i Answer
```

1.ii (2pt) the temperature of the air parcel is increased by 1 deg C, so
that it is no longer neutrally buoyant. Assuming the resulting
acceleration is maintained (i.e. constant) for 10 seconds, what is the
final parcel velocity?

```{code-cell} ipython3
# 1.ii Answer
```

1.iii (2 pt) Dry air at 1000 hPa is measured to have a temperature of
$27^\circ$ C, while at 900 hPa the temperature is 22 deg C, is this layer of the
atmosphere dry stable, neutral or unstable?

```{code-cell} ipython3
# 1.iii Answer
```

## 2. Fog

+++

In this question, for simplicity, assume the definition of relative humidity 
defined in terms of mixing ratio: RH = $r_v/r_{vsat}$

2.i (2pt) The environmental air with temperature of 25.0 deg C and a mixing
ratio of 10 $g kg^{-1}$ is located near the surface at a pressure of
1000hPa. What is the relative humidity?

```{code-cell} ipython3
 # 2.i answer
```

2.ii (1pt) The air starts to cool isobarically at night until even-
 tually a fog forms. Assuming the pressure is 1000 hPa. What is the
 temperature at which the fogs occurs? (Use the tephigram to answer
 this, please mark the answer on the tephigram)

```{code-cell} ipython3
# 2.ii answer
```

2.iii (2pt) Derive this same temperature by inverting Teton’s formula,
showing your working. (Out of interest note how close the answer is to
your tephigram result).

```{code-cell} ipython3
# 2.iii answer
```

iv (1pt) What is the common name given to this temperature?

```{code-cell} ipython3
# 2. iv answer
```

+++ {"jupyter": {"outputs_hidden": true}}

##  3. Relative humidity

+++ {"jupyter": {"outputs_hidden": true}}

In this question, for simplicity, assume the definition of relative
humidity defined in terms of mixing ratio.

+++ {"jupyter": {"outputs_hidden": true}}

3.i (2pt) A parcel of air at time t=0 has a temperature T0 of 25 deg C and a
relative humidity RH of 0.1. What is the mixing ratio $r_{v0}$?

```{code-cell} ipython3
# 3.i answer
```

+++ {"jupyter": {"outputs_hidden": true}}

3. ii (4pt) The parcel is cooled and moistened isobarically by the evaporation of precipitation to reach a final temperature 
of $T_1$ and a final mixing ratio of $r_{v1}$.  Use a rootfinder to solve for $T_1$ if the relative humidity is RH=0.6.

```{code-cell} ipython3
# 3.ii answer
```

+++ {"jupyter": {"outputs_hidden": true}}

## 4. Two compartment mixing

+++ {"jupyter": {"outputs_hidden": true}}

4.i (3pt) Consider two compartments, each with a volume of 1 $m^3$, separated by a rigid, perfectly insulating membrane.

+++ {"jupyter": {"outputs_hidden": true}}

- Initially $T_A$ = 300 K and $p_A$ = $10^5$ Pa, and $T_B$ = 100 K and $p_B$ = $10^3$ Pa.  Suppose the membrane breaks.  Find the final temperature and pressure in the 2 $m^3$ compartment.   Does the total entropy change?  By how much?

```{code-cell} ipython3
# 4.ii answer
```

## 5. Taylor series

+++

Find the second order taylor series expansion for the following functions:

+++

5.i (2pt) $(1 + \delta)^{-3}$

```{code-cell} ipython3
# 5.i answer
```

5.ii(2pt) $\log(1 + \delta)$

```{code-cell} ipython3
# 5.ii answer
```
